# Model Serving Framework
kserve==0.13.1

# Deep Learning
# tensorflow-cpu: Use CPU inference (don't need GPU in serving)
# tf-keras: keras 2.x API integrated with TF 2.15 (train.py uses from tensorflow.keras)
# Do not use standalone keras==2.x at the same time as tensorflow (cause conflict)
tensorflow-cpu==2.15.0
tf-keras==2.15.0

# Data Processing
numpy>=1.24,<2.0
pandas>=2.0
scikit-learn>=1.3
scipy>=1.11          # runtime dependency of scikit-learn
joblib>=1.3

# Image Processing
# Pillow: dùng Image.open, ImageEnhance.Contrast (khớp preprocess_image.py)
# opencv-python-headless: dùng cv2.cvtColor, cv2.createCLAHE, cv2.GaussianBlur
Pillow>=10.0
opencv-python-headless>=4.8

# Model Artifact Loading
# h5py: đọc file .h5 (best_model_isic2024.h5)
h5py>=3.9
